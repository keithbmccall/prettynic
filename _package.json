{
  "name": "prettynic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "scripts/launch.sh",
    "upgrade": "scripts/upgrade.sh",
    "upstart": "scripts/upgrade.sh && scripts/launch.sh",
    "stage": "scripts/stage.sh",
    "pull": "git pull origin main",
    "hp": "git checkout staging && git subtree push --prefix clientz heroku main && git checkout main"
  }
}
