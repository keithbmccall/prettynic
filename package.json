{
  "name": "prettynic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "scripts/launch.sh",
    "upgrade": "scripts/upgrade.sh",
    "upstart": "scripts/upgrade.sh && scripts/launch.sh",
    "pull": "git pull origin main",
    "heroku-push": "git checkout staging && git subtree push --prefix clientaaaa heroku main && git checkout main"
  }
}
